name: Slideshow
options:
  bundleIdPrefix: is.kte
  deploymentTarget:
    macOS: "26.0"
  xcodeVersion: "26.3"
  generateEmptyDirectories: true
  groupSortPosition: top

settings:
  base:
    SWIFT_VERSION: "6"
    SWIFT_STRICT_CONCURRENCY: complete

packages:
  SlideshowKit:
    path: SlideshowKit

targets:
  Slideshow:
    type: application
    platform: macOS
    sources:
      - path: Slideshow
        excludes:
          - "*.entitlements"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: is.kte.slideshow
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_FILE: Slideshow/Info.plist
        CODE_SIGN_ENTITLEMENTS: Slideshow/Slideshow.entitlements
        GENERATE_INFOPLIST_FILE: true
    dependencies:
      - package: SlideshowKit

  SlideshowUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - path: SlideshowUITests
    settings:
      base:
        GENERATE_INFOPLIST_FILE: true
    dependencies:
      - target: Slideshow

schemes:
  Slideshow:
    build:
      targets:
        Slideshow: all
        SlideshowUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - SlideshowUITests
